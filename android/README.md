# MyPhoneLibrary Android

Native Android client, derived from the existing MyMediaLibrary client. Local and Tailscale addresses, connection QR scanning, persistent WebView login, file uploads/camera, downloads and reconnection. The Windows server stores the collection. Signed with the existing private CI release key; keys are never committed.
